# 领域上下文

本文件记录跨功能稳定的业务术语、领域规则和架构决策入口。

## 核心术语

- **AI 导入**：把结构化的自然语言测试用例转化为可审阅、可编辑并可生成测试代码的测试计划的系统能力。
- **测试意图（TestIntent）**：一条测试用例的业务规格，由有序的意图步骤组成，不描述页面组件、定位器或具体浏览器操作。
- **意图步骤（Intent Step）**：测试意图中的一个业务动作或断言，可以引用一个或多个自然语言来源行。
- **操作组（Action Group）**：实现一个意图步骤的有序低层操作集合。
- **Action IR**：由封闭 Playwright 原语、结构化定位器和可观察等待条件组成的可执行中间表示；系统据此确定性生成测试代码。
- **页面档案**：按项目环境和页面保存的证据包，包含语义说明、成功验证的操作记录以及按需采集的页面证据，供多个测试计划复用；它不是对所有页面组件的完整建模。
- **页面状态**：用户完成一次操作后可观察和继续交互的页面形态；同一 URL 下的主页面、弹窗、标签页或权限视图可以是不同页面状态，测试计划可以跨页面状态连续执行。
- **页面目标（Page Target）**：页面状态中具有业务含义、并已在真实页面验证 Locator 的交互或断言对象；页面档案只记录实际需要的页面目标，不维护完整元素目录。
- **操作配方（Operation Recipe）**：在指定页面状态下验证成功、可按适用参数复用的低层操作序列，可以产生另一个页面状态。
- **探索租约（Exploration Lease）**：按项目环境、页面档案和探索范围建立的互斥任务占用；同一租约同一时间只允许一个浏览器探索会话，其他用例等待并复用结果。
- **测试计划**：由测试意图、操作组和自然语言来源引用组成的规范化测试模型，是录制、手动编辑和 AI 导入共同产出的可编辑结果。
- **测试代码**：由测试计划生成并实际执行的 Playwright 测试程序，是自动化测试的执行对象。
- **测试资产（Test Asset）**：由项目统一管理并供测试计划引用的上传文件；文件内容按内容摘要去重保存，逻辑文件名与物理内容分离。

## 稳定规则

- AI 只生成候选测试计划，不负责最终测试结果的判定。
- Agent 不能修改应用源码或正式业务数据，只能在独立任务工作区读取受控输入、写入候选结果和过程资料；系统通过结构校验后写入正式数据。
- 测试结果必须由测试代码执行并判定；AI 不能同时作为测试选手和裁判。
- 录制、手动编辑和 AI 导入应产出同一种测试计划，以便用户统一审阅和微调。
- 每条测试计划只以一个 `case.json` 作为正式数据源；Excel 解析结果、Agent 中间输出和探索过程资料属于导入任务临时数据。
- 测试意图中的业务动作和断言按实际顺序保存在同一个步骤数组中；业务动作只描述目标、数据和补充说明，不记录低层交互过程。
- Excel 来源行与意图步骤允许多对多映射；意图步骤使用稳定标识，并保留工作表、行号和原始业务字段，数组位置和 Excel 行号都不充当对象标识。
- 一个意图步骤至多对应一个操作组，操作组可以包含多个 Action IR 动作；修改意图步骤时立即移除对应操作组，重新生成前不得执行旧动作。
- AI 导入、录制和手动创建只在来源信息上有所区别，不得形成不同的编辑、生成或执行流程。
- 测试计划允许保存未解决的待确认项并保持草稿状态；代码审查和测试运行结果是独立产物，不写入 `case.json`。
- 页面档案按需增量探索并允许用户指定刷新；刷新后的页面事实供后续测试计划复用。
- 用户按页面刷新页面档案，不需要感知页面状态；系统刷新该页面的全部已知状态。
- 页面档案刷新成功后替换当前版本并保留最近一次旧版本；刷新失败时继续使用旧版本并标记失败。
- 页面档案按项目环境和业务路由模式归并；动态路径参数和不改变页面类型的查询参数不拆分页面。
- 页面档案记录页面的直接地址或从其他页面进入所需的操作路径，以支持没有独立 URL 的页面状态。
- 页面档案只服务于测试计划生成时的事实复用；删除或刷新页面档案不改变已经生成的测试计划和测试代码。
- 页面档案只把验证成功的低层操作记录用于后续生成；探索失败记录保留在导入任务日志中，不合入可复用档案。
- 只有可操作内容明显变化时才建立新的页面状态；普通字段值变化和短暂提示作为当前状态中的观察结果处理。
- 页面档案按需保存可访问性快照和相关区域截图，不保存 DOM 片段、完整网络正文或浏览器凭据。
- 页面探索可以执行测试用例所需的有副作用操作，项目环境必须允许测试过程修改数据。
- 页面探索优先复用项目已经保存的登录态；登录失效、验证码或多因素认证阻塞探索时，任务暂停并等待用户更新登录态后重试。
- 断言的期望结果必须来自用户提供的自然语言测试用例；页面探索只能确定断言对象和定位方式，不能把观察到的当前行为定义为正确结果。
- 首版测试计划只包含可编译为固定 Playwright 代码的线性步骤，不包含条件分支、循环或运行时 AI 决策。
- 测试意图使用业务动作类型，不要求用户描述页面实现；Agent 结合页面档案和实际探索，将其展开为使用封闭原语的 Action IR。
- 测试计划保存执行所需的 Action IR，生成后不依赖页面档案，也不在运行时调用 AI。
- 一个用户操作产生多个预期结果时，每个预期结果保存为独立断言步骤，以便分别执行和报告。
- 测试计划的前置条件只用于描述上下文和人工审阅，不直接执行；需要执行的准备动作必须保存为步骤。
- 测试计划不保存项目环境地址；起始路径在运行时与所选项目环境组合。
- 测试计划使用显式页面引用区分主页面和新窗口，步骤不得依赖对当前页面的隐式猜测。
- 步骤默认使用 Playwright 自动等待；显式等待必须描述可观察条件，不允许固定时长等待。
- 首版测试数据只保存普通字面值，不提供变量、数据集或密钥引用。
- 自然语言测试用例模板优先使用受约束的选择项；描述仍有歧义或缺失关键条件时，测试计划标记为待确认，不由 AI 擅自补全。
- 自然语言测试用例模板使用“用例”和“步骤”两个工作表，表名与列名固定，不接受别名。
- 「用例」列：用例编号（文件内唯一）、用例名称、起始路径、前置条件、预期结果、备注。起始路径为相对路径，不保存项目环境地址。
- 「步骤」列：用例编号、步骤序号、动作类型、目标、数据、补充说明。动作类型为业务级封闭选项：打开页面、填写、选择、点击、检查可见、检查文本。
- 自然语言测试用例模板中一个用例可以关联多行有序步骤；固定字段使用选择项，不使用 Playwright 步骤类型。
- Excel 文件结构错误时阻断整次导入；单个用例内容错误时只阻断该用例，并返回工作表、行号和原因。
- 页面档案不按时间自动过期；缺少当前用例所需信息时自动增量探索，用户可以主动刷新整个页面档案。
- 测试计划依次通过结构校验、确定性代码生成和代码语法检查；运行生成代码时，业务断言失败作为测试结果保留，不允许 Agent 修改已锁定的预期结果。
- 一次 AI 导入只运行一个 Agent 任务，可以生成多个测试计划；多个用例涉及同一页面时合并页面探索。
- 一个导入任务可以运行最多 4 个同类型 OpenCode worker；每个 worker 使用独立的 Playwright MCP 进程和浏览器实例。页面探索按探索租约去重，同一页面档案的探索不会重复操作浏览器，不同页面可以并行探索。
- AI 导入任务在后台执行并逐项保存结果；中断或失败后只继续未完成项，不重复处理成功项。
- 导入任务检查点使用原子写入；恢复时以检查点和逐用例状态为准，不重新解析 Excel，也不按输入文件哈希复用整个导入任务。
- 测试资产按内容 SHA-256 保存在项目资产库，相同内容只存一份；导入任务通过资产标识引用，逻辑文件名与物理内容分离。
- 功能开发和验收可以使用当前 Vite + Node server 开发模式；正式发布提供包含固定 OpenCode、Playwright MCP 和运行环境的本地客户端包，不要求用户全局安装 CLI 或源码环境。
